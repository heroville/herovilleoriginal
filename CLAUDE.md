# Heroville Agent Instructions

## Game logic
Do not change game rules, formulas, economy, combat, or progression unless explicitly asked. You may suggest improvements or balance changes; wait for confirmation before implementing.

## Architecture
Redux is the single source of truth. Services read state via `store.getState()` and dispatch fine-grained actions — no separate mutable state objects. The game loop in `src/gameLoopRunner.ts` runs tick and save as Redux thunks at a configurable interval.

Service-level timers (dungeon travel, production crafting) use `setTimeout` internally and must dispatch Redux actions when they resolve.

## Testing
Regression gate is E2E: run `npm test` (builds, then runs Playwright) after any change and fix failures before finishing. Add `data-testid` attributes on key interactive elements when building new UI — use them in E2E selectors, not CSS classes or copy.

## E2E test hooks
`window.__HEROVILLE_E2E_FAST_TICK__` (sets tick speed 2–500ms) and `window.__HEROVILLE_E2E_STATE__` (exposes state for assertions) are used by Playwright. Keep them guarded by `import.meta.env.DEV` so they are stripped from production builds.

## These instructions
Do not edit this file without explicit user confirmation. Suggest changes inline or at the end of a task.
